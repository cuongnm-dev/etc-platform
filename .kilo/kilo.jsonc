{
  // etc-docgen project — Kilo Code configuration
  // MCP server: etc-docgen document generation engine
  // Requires: docker compose up -d (from D:\Projects\etc-docgen)
  "mcpServers": {
    "etc-docgen": {
      "type": "sse",
      "url": "http://localhost:8000/sse",
      "disabled": false
    }
  }
}
